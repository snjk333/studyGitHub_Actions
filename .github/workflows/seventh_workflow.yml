name: Seventh Workflow

## DOC : https://crontab.cronhub.io/
on:
  schedule:
    - cron: '*/5 * * * *' # Runs every 5 minutes
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Event variables
        run: |
          echo $GITHUB_SHA 
          echo $GITHUB_REF
          